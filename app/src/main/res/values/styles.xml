<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/myLightBlue</item>
        <item name="colorPrimaryDark">@color/myDarkBlue</item>
        <item name="colorAccent">@color/myYellow</item>
        <item name="android:state_checked">@color/myMiddleBlue</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- ToolBar -->
    <style name="ToolBarStyle" parent="Theme.AppCompat">
        <item name="colorPrimary">@color/myLightBlue</item>
        <item name="android:textColorPrimary">@color/myDarkBlue</item> <!--changes app name-->
        <item name="android:textColorSecondary">@color/myDarkBlue</item> <!--changes icons-->
        <!--<item name="android:background">@color/myLightBlue</item> changes background close to name-->
        <item name="actionMenuTextColor">@color/myDarkBlue</item>
        <!-- <item name="actionOverflowButtonStyle">@style/ActionButtonOverflowStyle</item>
         <item name="drawerArrowStyle">@style/DrawerArrowStyle</item> -->
    </style>

</resources>
